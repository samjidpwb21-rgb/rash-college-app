# Database Connection
# Replace with your PostgreSQL connection string
DATABASE_URL="postgresql://user:password@localhost:5432/college_attendance"

# NextAuth Configuration
# Generate a secret with `openssl rand -base64 32`
NEXTAUTH_SECRET="change_me_to_a_secure_random_string"
NEXTAUTH_URL="http://localhost:3000"

# Optional: Set to 'production' in deployed environments
NODE_ENV="development"

# Email Service (for OTP verification)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="samjidp4@gmail.com"
SMTP_PASS="cinu jsco vyal nbbr"
SMTP_FROM="R.A.S.H College App <samjidp4@gmail.com>"
